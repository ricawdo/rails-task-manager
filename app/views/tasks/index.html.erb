<ul>
<% @tasks.each do |task| %>
<li>
<%= link_to task.title, task_path(task.id) %>
<%= link_to "Delete",
            task_path(task.id),
            method: :delete,
            data: { confirm: "Are you sure?" }%>
<% end %>
</li>
</ul>
<%= link_to "Add", new_task_path  %>
